{
  "name": "server",
  "version": "1.0.0",
  "description": "server",
  "main": "app.js",
  "engines": {
    "node": "19.4.0"
  },
  "scripts": {
    "start": "nodemon app.js",
    "start:prod": "node app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "mongoose": "^7.0.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.20"
  }
}
